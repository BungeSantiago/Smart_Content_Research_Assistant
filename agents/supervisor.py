"""
Supervisor Agent.

En LangGraph, el supervisor es esencialmente el grafo mismo: define el flujo
y las transiciones. No es un nodo en sí, sino la estructura que orquesta
los demás. Acá vamos a definir helpers si los necesitamos más adelante.
"""
# Por ahora vacío; el rol del supervisor lo cumple el grafo en core/graph.py